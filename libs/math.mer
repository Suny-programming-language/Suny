let add(x, y) = (function() do
    return x + y
end)()

let mul(x, y) = (function() do
    return x * y
end)()

let sub(x, y) = (function() do
    return x - y
end)()

let div(x, y) = (function() do
    return x / y
end)()
